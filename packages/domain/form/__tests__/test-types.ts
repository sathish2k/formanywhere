/**
 * Local type re-exports for test files.
 * Re-exports from the self-contained shim to provide all types tests need.
 */
export type {
    FormElement,
    FormElementType,
    ValidationRule,
    ConditionalRule,
    FormSchema,
    FormSettings,
    FormPage,
    FormSubmission,
    RuleCondition,
    RuleAction,
    FormRule,
    WorkflowNode,
    WorkflowEdge,
    FormWorkflow,
    WorkflowExecutionResult,
    WorkflowValidationResult,
} from './shared-types-shim';
