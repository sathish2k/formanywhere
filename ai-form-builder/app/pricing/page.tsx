/**
 * Pricing Page
 */

import { Pricing } from '@/components';

export const metadata = {
  title: 'Pricing | FormBuilder AI',
  description: 'Simple, transparent pricing for FormBuilder AI',
};

export default function PricingPage() {
  return <Pricing />;
}
