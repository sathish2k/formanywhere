/**
 * Dashboard Page
 */

import { Dashboard } from '@/components';

export const metadata = {
  title: 'Dashboard | FormBuilder AI',
  description: 'Manage your forms and create new ones',
};

export default function DashboardPage() {
  return <Dashboard />;
}
