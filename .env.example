# ─────────────────────────────────────────────────────────
# FormAnywhere — Environment Variables
# Copy this file to .env and fill in real values.
#
#   cp .env.example .env
# ─────────────────────────────────────────────────────────

# ── Domain (REQUIRED) ───────────────────────────────────
# Your domain name WITHOUT https:// prefix
# Nginx will serve:  yourdomain.com  →  frontend
#                    api.yourdomain.com  →  backend
DOMAIN=yourdomain.com

# ── PostgreSQL ───────────────────────────────────────────
POSTGRES_USER=formanywhere
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here
POSTGRES_DB=formanywhere

# ── Redis ────────────────────────────────────────────────
REDIS_PASSWORD=CHANGE_ME_redis_password_here

# ── Auth ─────────────────────────────────────────────────
# REQUIRED — secret for signing sessions (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=CHANGE_ME_generate_a_random_secret

# ── SSL Certificate Email ───────────────────────────────
# Email for Let's Encrypt SSL certificate notifications
SSL_EMAIL=you@yourdomain.com

# ── OAuth Providers (optional) ───────────────────────────
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://api.yourdomain.com/api/auth/callback/google

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=https://api.yourdomain.com/api/auth/callback/github

# ── AI Keys (optional) ──────────────────────────────────
GEMINI_API_KEY=
XAI_API_KEY=

# ── Unsplash (optional — blog cover images) ─────────────
UNSPLASH_ACCESS_KEY=

# ── Email (optional — Resend for transactional emails) ──
# Without this, password reset emails are logged to console.
RESEND_API_KEY=
EMAIL_FROM=FormAnywhere <noreply@yourdomain.com>

# ── Error Tracking (optional — Sentry) ──────────────────
SENTRY_DSN=

# ── Analytics (optional — set in apps/web/.env) ─────────
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX  (set in apps/web/.env)
# VITE_ADSENSE_CLIENT_ID=ca-pub-XXX    (set in apps/web/.env)

# ── Logging ─────────────────────────────────────────────
# LOG_LEVEL=info  (debug | info | warn | error | fatal)
